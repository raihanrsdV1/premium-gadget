import React from 'react';

const CheckoutStepper = ({ currentStep }) => {
  return (
    <div className="flex items-center space-x-4">
      {/* Checkout Stepper Stub */}
      Step: {currentStep}
    </div>
  );
};

export default CheckoutStepper;
